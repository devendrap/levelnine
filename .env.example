# OpenAI — required for provider "openai"
OPENAI_API_KEY=

# xAI (Grok) — required for provider "xai"
XAI_API_KEY=

# Google Gemini — required for provider "gemini"
GEMINI_API_KEY=

# Ollama and Mistral use a local Ollama instance (localhost:11434) and need no API key.
